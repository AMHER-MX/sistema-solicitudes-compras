# Copia este archivo como .env solo si necesitas apuntar a otra API.
#
# En desarrollo NO hace falta: Vite ya reenvía /api a http://localhost:4000
# (ver la sección `proxy` en vite.config.js).
#
# En producción, apunta a la URL pública de tu API:
# VITE_API_URL=https://api.tu-dominio.com/api
